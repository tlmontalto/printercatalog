<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Monda&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/show.css">
    <title>Display Page</title>
</head>
<body>
        <header>
                <h2 id="printer-catalog">Filament Catalog</h2>
                <p id="username">Welcome, <%= currentUser.username %></p>
                <form action="/sessions?_method=DELETE" method="POST">
                    <input id="log-out" type="submit" value="Log Out" class="btn-small red" />
                </form>
                <button id="return"><a id="return-link" href="/filament">Return to Index</a></button>
        </header>
        <main>
        <h1 id="title"><%= filament.color %> <%= filament.brand %> <%= filament.type %></h1>
<div class=item>
        <img src="<%= filament.img %>">
</br>
<div class="details">
        <p class="line">Type: <%= filament.type %></p>
        <p class="line">Brand: <%= filament.brand %></p>
        <p class="line">Color: <%= filament.color %></p>
        <% if (filament.stock > 0) { %>
                <p class="line">Stock: <%= filament.stock %>kg</p>
        <% } else { %>
                <p class="line">Out of Stock</p>
        <% } %>
        <p class="line">Hotend Temperature: <%= filament.hotendTemperature %>°C</p>
        <p class="line">Bed Temperature: <%= filament.bedTemperature %>°C</p>
        <p class="line">Print Speed: <%= filament.speed %>mm/s</p>
        <p class="line">Retraction Distance: <%= filament.retractionDistance %>mm</p>
        <p class="line">Retraction Speed: <%= filament.retractionSpeed %>mm/s</p>
        <p class="line"><a href="<%= filament.link %>" target="_blank">Purchase</a></p>
</div>
</div>
<div class="buttons">
        <a href="/filament/<%= filament.id %>/edit">Edit Filament</a>
        <form action="/filament/<%= filament.id %>?_method=DELETE" method="POST">
        <input type="submit" value="DELETE">
      </form>
</div>
        </main>
</body>
</html>